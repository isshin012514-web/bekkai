# bekkai 利用ガイド

「答えのない問い」に自分なりの答えを出し、それを実現するための自己分析アプリです。
5つの力（成長力・発見力・別解力・実現力・失敗力）を1つにまとめています。

---

## 1. アクセス方法

### 公開URL（どの端末でもOK）
🔗 **https://bekkai.vercel.app**

- スマホ・PC・タブレット、どのブラウザからでも開けます。
- インストール不要。ホーム画面に追加すればアプリのように使えます。
  - **iPhone (Safari)**: 共有ボタン → 「ホーム画面に追加」
  - **Android (Chrome)**: メニュー → 「ホーム画面に追加 / アプリをインストール」

> データは**その端末のブラウザ内（localStorage）に保存**されます。
> サーバーには送られないので、他人に見られることはありません。
> その代わり、**別の端末には自動では引き継がれません** → 下記「データ移行」を使います。

---

## 2. 機能（タブ）の使い方

| タブ | 役割 | ひとことで |
|------|------|-----------|
| 📈 **成長力** | INPUT→OUTPUT→採点→次の打ち手 のサイクル | アウトプットで自分を伸ばす |
| 🔍 **発見力** | 8視点の自己分析マンダラ | 「解くべき問題」を見つける |
| ✨ **別解力** | 自分らしい×優れた×別の の掛け合わせ | 自分なりの答えを出す |
| 🚀 **実現力** | 組み合わせ→量→質→チーム | 別解を「実現」する |
| ⚠️ **失敗力** | リスク設計・撤退ジャッジ・メタ認知 | 無防備に転ばない |
| 🗄️ **データ** | バックアップ・復元・AI壁打ち | 移行と活用 |

各タブの右上「?」アイコンから、その機能の使い方ガイドをいつでも開けます。

### 🚀 実現力（新機能）
別解は実現しなければ独りよがり。3つの観点で形にします。
1. **組み合わせ・連鎖** — 要素を掛け合わせて新しいイチをつくる。失敗したら自分らしさを残し、バランス／組み合わせ／濃度を変えて修正。
2. **量 → 質** — 量をこなして質に転化。正比／Jカーブ／ループの3パターンで、1万時間を目処に逆算。
3. **チーム・実行力** — 1000点はチームで取る。信用（成果）と信頼（人柄）を可視化し、無形の資産（友人・顧客）もフル活用。「根拠のない自信」を宣言しておく。

> 初回はデモデータが入っています。`+` ボタンから自分のデータを追加できます。

---

## 3. データ移行（端末を変える・URLが変わるとき）

データは端末ごとに保存されるため、移行は **JSONファイルの書き出し → 読み込み** で行います。

### 移行手順
1. **【移行元の端末】** で「🗄️ データ」タブを開く
2. **「バックアップ（書き出し）」** をタップ → `bekkai-backup-YYYY-MM-DD.json` がダウンロードされる
3. そのファイルを移行先の端末に渡す（AirDrop・メール・クラウド等）
4. **【移行先の端末】** で「🗄️ データ」タブを開く
5. **「復元（読み込み）」** をタップ → 受け取ったJSONファイルを選択
6. 「復元: 成長力・発見力・別解力」と表示されれば完了（実現力・失敗力も成長力に含めて移行されます）

> - 1つのJSONに**5機能すべて**のデータが入ります。
> - 復元すると**既存データは上書き**されます。念のため復元前にバックアップを取ると安全です。
> - 旧バージョンのバックアップ（実現力なし）も読み込めます（後方互換）。

### AI壁打ち
「🗄️ データ」タブの **「AIに相談」** から、自己分析データを読みやすい文章にまとめ、ChatGPT / Claude にそのまま貼り付けて相談できます。

---

## 4. 開発・再デプロイ（開発者向け）

```bash
# ローカル開発
npm install
npm run dev        # http://localhost:5174

# 本番ビルド
npm run build      # dist/ に出力

# Vercel へ本番デプロイ（公開URLを更新）
vercel deploy --prod --yes
```

- ホスティング: **Vercel**（プロジェクト名 `bekkai` / 本番エイリアス `bekkai.vercel.app`）
- リポジトリ: `github.com/isshin012514-web/bekkai`
- SPA設定・ビルド設定は `vercel.json` に記載。
- モバイルアプリ化は Capacitor 対応済み（`npm run cap:ios` / `npm run cap:android`）。

---

_最終更新: 2026-06-10 / 実現力タブ追加・Vercel公開時点_
